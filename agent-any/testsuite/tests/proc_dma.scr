.

`anyagent(
    Description (
      ``(File ("tests/proc_dma.in")),	// real file name
	"#\n",			// Comment
	true,				// read-only
	``(Tuple (Optional (Whitespace()), Value (Number()), ": ",
	Name(String("^\n")), Continue ("\n")))
    )
)
