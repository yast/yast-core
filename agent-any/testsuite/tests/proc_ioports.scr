.

`anyagent(
    Description (
    ``(File ("tests/proc_ioports.in")),	// real ``File name
  "#\n",			// Comment
  true,				// read-only
  ``(Tuple (Value (List(Hexval(),"-")), " : ", Name(String("^\n")), Continue
    ("\n")))
    )
)
