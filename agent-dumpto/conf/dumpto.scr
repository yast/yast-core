/**
 * File:
 *   dumpto.scr
 * Summary:
 *   SCR Agent for dumping data structures to files
 * Access:
 *   write-only
 * Authors:
 *   Unknown <yast2-hacker@suse.de>
 * See:
 *   libscr
 * Example:
 *   Write(.dumpto.tmp.dumpfile, Read(.etc.fstab))
 *   (true)
 **
 *   /tmp/dumpfile:
 *   [
 *     $[
 *       "file" : "/boot",
 *       "freq" : 1,
 *       "mntops" : "defaults",
 *       "passno" : 2,
 *       "spec" : "/dev/sda1",
 *       "vfstype" : "ext2"
 *     ],
 *     $[
 *       "file" : "swap",
 *       "freq" : 0,
 *       ...
 *     ],
 *     ...
 *   ]  
 * 
 * $Id$
 *
 * <p>This agent allows to dump any ycp data-structure to a file. The path
 * to the file is appended to the mountpoint, the datastructure is passed
 * as argument.</p>
 * <p>Return true on success, false on failure.</p>
 */
.dumpto

`ag_dumpto ()
