#
# ag_ini.exp
# 'main' file for all ag_ini.tests
#

# get all files matching tests/*.ycp

set filenames [glob $srcdir/debug/*.ycp]

# foreach file, call ycp-run (from testsuite/lib)

foreach file $filenames { ycp-run debug $file }
