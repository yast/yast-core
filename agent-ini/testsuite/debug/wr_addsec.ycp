{
    // test of adding values, sections, subsections

    // The file is not saved if not changed. We must change something
    // and flush it!
    // if file is not changed, then Write (., nil) makes log entry
    // otherwise write is silent.
    Write (.s.Main.Sub.Sub.Sub, "; Comment\n");
    y2error ("Writting:");
    Write (., nil);
    y2error ("Destructor:");
}
