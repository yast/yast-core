//
// Multiple files test
//
// providers.rpm
//


{
    list l = Dir (.s);
    y2milestone ("%1", l);
    foreach (`i, l, ``{
	y2milestone ("%2: %1", Dir (add (.s, i)), i);
    });
    return nil;
}
