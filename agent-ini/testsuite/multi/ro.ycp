//
// Multiple files test
//
// providers.rpm
//
// read-only test.


{
    Write (.s.new, "newsection");
    list l = Dir (.s);
    y2milestone ("%1", l);
    foreach (`i, l, ``{
	y2milestone ("%2: %1", Dir (add (.s, i)), i);
    });
    return nil;
}
