{
    // comment on line has lower priority then value parsing

    // The file is not saved if not changed. We must change something
    // and flush it!
    Write (., "force");
    return Dir (.s);
}
