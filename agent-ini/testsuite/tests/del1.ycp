{
    // deleting of values

    // The file is not saved if not changed. We must change something
    // and flush it!
    SCR::Write (.v.Section1.Sub1.Vals1, nil);
    SCR::Write (.s.Section1.Sub2, nil);
    SCR::Write (.s.Section2, nil);
    SCR::Write (.s.Section0, nil);

    SCR::Write (., "force");

}
