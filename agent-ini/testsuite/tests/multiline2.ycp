{
    // multiline values example, joins values into one line

    // The file is not saved if not changed. We must change something
    // and flush it!
    Write (., "force");
    return Dir (.s);
}
