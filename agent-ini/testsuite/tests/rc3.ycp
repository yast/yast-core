// Dir on leaves should not return Dir of their parent directory!
// #21574
{
    y2error ("%1", Dir (.));
    y2error ("%1", Read (.DEFAULT_LANGUAGE));
    y2error ("%1", Dir  (.DEFAULT_LANGUAGE));
    return Dir (.);
}
