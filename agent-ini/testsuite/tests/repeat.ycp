/*
 * Section names/key names are not unique.
 * Sections with the same names are merged.
 * The last value is used in case of non-unique keys.
 */
{
    SCR::Write (., "force");
    return SCR::Dir (.s);
}
