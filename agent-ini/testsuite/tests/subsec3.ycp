{
    // subsections, test of bad section closing,
    // unclosed sections

    // The file is not saved if not changed. We must change something
    // and flush it!
    Write (., "force");
    return Dir (.s);
}
