{
    // test of reading type of section/value

    y2milestone ("%1", Read (.st.section1));
    y2milestone ("%1", Read (.st.section2));
    y2milestone ("%1", Read (.st.section3));
    y2milestone ("%1", Read (.vt.section1.key1));
    y2milestone ("%1", Read (.vt.section1.key2));
    y2milestone ("%1", Read (.vt.section2.key3));
    y2milestone ("%1", Read (.vt.section3.key1));
    y2milestone ("%1", Read (.vt.section3.key2));
    y2milestone ("%1", Read (.vt.section3.key3));

    Write (.st.section1, 1);
    Write (.st.section2, 2);
    Write (.st.section3, 0);
    Write (.vt.section1.key1, 1);
    Write (.vt.section1.key2, 2);
    Write (.vt.section2.key3, 0);
    Write (.vt.section3.key1, 1);
    Write (.vt.section3.key2, 2);
    Write (.vt.section3.key3, 0);

    y2milestone ("%1", Read (.st.section1));
    y2milestone ("%1", Read (.st.section2));
    y2milestone ("%1", Read (.st.section3));
    y2milestone ("%1", Read (.vt.section1.key1));
    y2milestone ("%1", Read (.vt.section1.key2));
    y2milestone ("%1", Read (.vt.section2.key3));
    y2milestone ("%1", Read (.vt.section3.key1));
    y2milestone ("%1", Read (.vt.section3.key2));
    y2milestone ("%1", Read (.vt.section3.key3));

    return Dir (.s);
}
