/**
 * File:
 *   mnt_modules.scr
 * Summary:
 *   SCR Agent for reading/writing /mnt/etc/modules.conf
 * Access:
 *   read/write
 * Authors:
 *   Michal Svec <msvec@suse.cz>
 *   Daniel Vesely <dan@suse.cz>
 * See:
 *   libscr
 *   man modules.conf
 *   Mountpoint .modules
 *   y2a_mods/modules.html
 * Example:
 *   Read(.mnt.modules)
 *   (["alias", "options", "post-install", "pre-install", "pre-remove"])
 **
 *   Read(.mnt.modules.options)
 *   (["agpgart", "cipcb", "dummy0", "dummy1", "parport_pc", "vtx"])
 **
 *   Read(.mnt.modules.options, "agpgart")
 *   ($["agp_try_unsupported":"1"])
 **
 *   Write(.mnt.modules.options, $["agp_try_unsupported":"0"], "agpgart")
 *   (true)                                                                     
 * 
 * $Id$
 *
 * <p>Same as .modules just for accessing /mnt/etc/modules.conf</p>
 *
 * <p>Reads/writes <tt>/mnt/etc/modules.conf</tt>. For more detail see its 
 * own documentation.</p>
 */
.tmp.modules

`ag_modules(
  ModulesConf("/tmp/modules.conf")
)
