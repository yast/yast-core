{
    return Read (.byte, "tests/data1.read");
}

{
    return Read (.byte, "tests/data2.read");
}

{
    byteblock data = Read (.byte, "tests/data2.read");
    return Write (.byte, "tmp.write.byte", data);
}

{
    # this must produce a error in the log
    return Read (.byte, "tests/not-here.data");
}

{
    # this must not produce a error in the log
    return Read (.byte, [ "tests/not-here.data", #[ff] ]);
}
