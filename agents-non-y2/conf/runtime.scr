/**
 * File:
 *   runtime.scr
 * Summary:
 *   SCR Agent for reading username information
 * Access:
 *   read-only
 * Authors:
 *   Unknown <yast2-hacker@suse.de>
 * See:
 *   libscr
 *   man whoami
 * Example:
 *   Read(.runtime.username)
 *   ("root")
 **
 * 
 * $Id$
 *
 * <p>Script that reads username with `whoami`</p>
 */
.runtime

`ag_runtime ()
