/**
 * File:
 *   xauth.scr
 * Summary:
 *   SCR Agent for reading/writing xauthority information
 * Access:
 *   read/write
 * Authors:
 *   Michal Svec <msvec@suse.cz>
 * See:
 *   libscr
 *   man xauth
 * Example:
 *   Read(.xauth.key)
 *   ("/root/.Xauthority-2cmyRF")
 **
 *   Write(.xauth.key, "/root/.Xauthority-2cmyRF")
 *   (true)
 *
 * $Id$
 *
 * <p>Script that reads key number of $USER from xauthority
 * used for correct changing hostname
 * Read the key BEFORE changing hostname
 * Write key AFTER changing hostname</p>
 */
.xauth

`ag_xauth ()
