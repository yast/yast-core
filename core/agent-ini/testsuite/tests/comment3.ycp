{
    // trailing comment changes to leading in empty file

    // The file is not saved if not changed. We must change something
    // and flush it!
    SCR::Write (.s.Section, "");
    SCR::Write (., "force");

}
