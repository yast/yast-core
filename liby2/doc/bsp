y2qt mainmenu

	Startet das Qt Interface mit dem Modul 'mainmenu'

y2qt mainmenu -geometry 17x20

	Startet das Qt Interface mit dem Modul 'mainmenu' und uebergibt
	dem Qt server die Parameter [ "-geometry", "17x20" ]

y2qt
	Macht garnichts oder produziert einen Hilftext. Man
	muss ein Modul mitangeben, oder zumindest eine Macrodatei laden.
	Das Userinterface bekommt sonst keinen Input.

y2qt stdio

	Startet das Qt Interface und nimmt stdio als Modulkanal.
	Man kann jetzt direkt YCP eintippen, wie bisher.
        Man kann auch eine Textdatei mit YCP hineinpipen. stdio
	ist ein Schluesselwort, kein Modulname.

y2qt -p qtmacros.ycp

	Startet das Qt Interface, parst die Datei qtmacros.ycp
	und schickt das geparste Script der UI zur Ausfuehrung.
	Der Rueckgabewert der Ausfuehrung wird ignoriert. Anschliessend
	wird das Qt Interface beendet.

y2qt -e '{ ShowDialog(`Copyright()); UserInput(); CloseDialog();  }'

	Startet das Qt Interface, parst den String zur -e Option,
	schickt das geparste YCP Script dem Qt zur Ausfuehrung,
	beendet anschliessend das Qt Interface.

y2qt userman

	Startet das Qt Interface und das Modul 'userman' und baut
	eine Kommunikation zwischen beiden auf. Das Module beendet
	die Session, wenn es fertig ist.

y2qt /lib/YaST2/modules/mainmenu.ycp

	Startet das Qt Userinterface und das Modul, das durch das YCP
	Scriptfile /lib/YaST2/modules/mainmenu.ycp definiert ist.

y2cat userman

	Startet das Modul 'userman', verwendet aber anstelle eines
	UserInterfaces stdio.  Auf diese Art kann man zum einen das
	Module userman testen, zum anderen kann man es z.B. per
	inetd dem Netz zur Verfuegung stellen.

y2userman 'add(kettner, 501, 71)'

	Startet das Modul 'userman' ohne user interface. Uebergibt
	ihm als Parameter den geparsten YCP Ausdruck add(kettner,501,71).
	Das Modul arbeitet im Batchbetrieb und liefert am Ende einen
	Exitcode.

y2userman

	Startet das Module 'userman' ohne user interface, uebergibt
	aber keine Parameter. Sobald das Modul interaktiv auf das
	UserInterface zugreifen will, wird mit einer Fehlermeldung
	abgebrochen, da keine Interaktion moeglich ist ohne
	Userinterface

y2qt userman 'add(kettner, 501, 71)'

	Startet das Modul 'userman' mit Qt als user interface. Uebergibt
	ihm als Parameter den geparsten YCP Ausdruck add(kettner,501,71).
	Das Modul arbeitet im Batchbetrieb und liefert am Ende einen
	Exitcode. Das Userinterface wurde zwar gestartet, aber nicht verwendet.
	Solange kein ShowDialog() auftrat, sieht der Benutzer kein
	Fenster.

y2scr stdio

	Eroeffnet eine Session mit dem SCR, wobei man auf stdin Befehle
	tippen kann und auf stdout Antworten bekommt. In dieser Form
	koennte man das SCR z.B. ueber den Inetdaemon verfuegbar machen.

y2qt Herstein.suse.de:userman

	Startet auf Rechner Herstein.suse.de das Module 'userman' und
	zeigt es mittels des Qt Interfaces an.

y2cat Herstein.suse.de:userman

	Startet auf Rechner Herstein.suse.de das Modul 'userman' und
	lenkt dess IO auf stdio um.

y2gf scr stdio

	Tut genau das gleiche wie 'y2scr stdio'

y2gf Herstein.suse.de:7111 userman

	Startet das Modul userman und verwendet als User Interface 
	auf Herstein den Port 7111. Das bedeutet, dass userman seine
	Befehle fuer die UI an Herstein, Port 7111 schickt.

y2qt wfm @/lib/YaST2/modules/mainmenu.ycp 17

	Startet mit dem Qt Interface das Modul 'wfm', den Workflowmanager.
	Dieser bekommt als ersten Parameter den geparsten Inhalt der Datei
	mainmenu.ycp und als zweiten Parameter die Zahl 17 als YCPInteger.
	Dieser Aufruf ist gleichbedeutend mit: y2qt mainmenu 17

y2wfm stdio -s

	Liest einen YCPValue von stdin (Option -s) und parst diesen.
	Startet dann den Workflowmanager und gibt den geparsten Wert
	als einziges Argument. 

