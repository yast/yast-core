Changes for YCP


- typed nil: nilstring, nillist, nilmap, ...
- typed lists: list(string)
- typed maps: map(string)
- lookup() must have a typed default
- select() must have a typed default
- symbols as parameters must be typed

- sort (list) -> sortlist (list)
