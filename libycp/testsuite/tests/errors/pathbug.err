[Scanner] tests/errors/pathbug.ycp:4 Warning: Useless 'global' outside of module
Parsed:
----------------------------------------------------------------------
{
    //T: TEntry (list2path@4:global path list2path())
    // global path list2path()
    // filename: "tests/errors/pathbug.ycp"
    global path list2path (list strs)
{
    // path p
    path p = .;
    if ((p != .))
    {
    y2error ("this is very strange, p is %1", p);
}
    foreach (any e, strs, {
    p = add (p, topath (e));
}
);
    return p;
}
    return (::list2path (["a"]) + ::list2path (["b"]));
}
----------------------------------------------------------------------
