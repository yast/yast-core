Parsed:
----------------------------------------------------------------------
{
  global define list2path(list strs) ``{
  path `p = .;
  if ((p != .)) {
  y2error ("this is very strange, p is %1", p);
}
  foreach (`e, strs, ``{
  `p = (add (p, e));
}
);
  return p;
}
;
  list2path (["a"]);
  list2path (["b"]);
}
----------------------------------------------------------------------
