Parsed:
----------------------------------------------------------------------
{
    // list l
    // list r
    // filename: "tests/statements/foreach_continue.ycp"
    list l = ["1", "2", "3"];
    list r = [];
    foreach (string s, l, {
    if ((s == "2"))
    continue;
    r = (r + s);
}
);
    return r;
}
----------------------------------------------------------------------
