{
    // new, easier syntax for UI:: statements

    // 1. drop double quoutes (still used internally)
    //
    // it's the same as writing UI::``{ .... }

    return UI::{
	return test();
    };


    // 2. C++ style member definitions
    //
    // this is equal to
    // UI::``{ global define test .... }

    global define UI::test () ``{
	return "defined inside UI";
    }

}
