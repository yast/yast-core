Parsed:
----------------------------------------------------------------------
{
    // boolean isString (any a)
    // list la
    // integer index
    // any s
    // filename: "tests/values/List.ycp"
    boolean isString (any a)
{
    if (is (a, string))
    {
    y2milestone ("%1 is a string", a);
    return true;
}
else
    {
    y2milestone ("%1 not a string", a);
    return false;
}
}
    list la = [3.14, "foo", 1];
    integer index = 1;
    any s = select (la, index, nil);
    return [! isString (la), isString (s), is (s, string), isString (select (la, index, nil)), is (select (la, index, nil), string)];
}
----------------------------------------------------------------------
[YCP] tests/values/List.ycp:11 [3.14, "foo", 1] not a string
[YCP] tests/values/List.ycp:6 foo is a string
[YCP] tests/values/List.ycp:6 foo is a string
