{
    UI::OpenDialog(
	       `VBox(
		     `Frame ( "Important",
			      `Label("Hello, World!")
			      ),
		     `PushButton("&OK")
		     )
	       );
    UI::UserInput();
    UI::CloseDialog();
}
