/**
 * Example for an animated image
 **/
{
    string movie = "/usr/share/YaST2/theme/current/animations/ticks-endless.gif";
    UI::OpenDialog(
		   `VBox(
			 `MinSize( 10, 4,
				   `Image(`opt(`animated), movie, "animation" ) ),
			 `PushButton("&OK")
			 )
		   );
    UI::UserInput();
    UI::CloseDialog();
};
