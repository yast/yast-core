UI::{
    /*
     * This will cause an internal error since UserInput() is called without
     * any dialog being opened.
     */
    
    while( true )
    {
	any ret = UserInput();
	y2milestone( "UserInput() returned %1", ret );
    }
}
