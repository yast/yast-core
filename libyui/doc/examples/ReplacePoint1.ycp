UI::{
    OpenDialog(
	       `VBox(
		     `ReplacePoint(`id(`rp), `Label("This is a label")),
		     `PushButton(`id(`change), "Change")));
    UserInput();
    ReplaceWidget(`id(`rp), `PushButton("This is a PushButton"));
    UserInput();
    ReplaceWidget(`id(`rp), `CheckBox("This is a CheckBox"));
    UserInput();
    ReplaceWidget(`id(`rp), `HBox(`PushButton("Button1"), `PushButton("Button2")));
    UserInput();
    CloseDialog();
}
