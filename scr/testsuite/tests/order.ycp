{
    list ret = [];
    
    # when a path is registered twice, the last one is used
    
    RegisterAgent (.foo, "tests/haha.scr");
    ret = add (ret, Read (.foo.a));
    
    RegisterAgent (.foo, "tests/hihi.scr");
    ret = add (ret, Read (.foo.a));
    
    return ret;
}
