{
  // callback2.ycp

  integer a = 42;

  // this assign 13 to a *in UI scope*
  //   but returns the value of a *from WFM scope*
  //  -> result is 42

  integer wfm_result = UI (``{ integer a = 13; return WFM::a; });

  // this calls SCR from UI
  // it's _almost_ (!!) identical to SCR(`Read(.probe.architecture))
  // but does _not_ evaluate the parameter in UI scope

  string ui_scr_result = UI (``{ return SCR::Read(.probe.architecture); });

  // this is an alternative to SCR(`Read(.probe.architecture))
  //   but does not evaluate the parameter in WFM scope

  string wfm_scr_result = SCR::Read(.probe.architecture);

  y2milestone ("wfm_result %1", wfm_result);
  y2milestone ("ui_scr_result %1", ui_scr_result);
  y2milestone ("wfm_scr_result %1", wfm_scr_result);

  return [wfm_result==a, ui_scr_result == wfm_scr_result];
}
